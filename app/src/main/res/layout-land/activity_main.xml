<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:weightSum="4"
    android:id="@+id/main_activity_land"
    android:background="@color/mainActivityBG_LAN">

    <FrameLayout
        android:id="@+id/frameLayout_contact_list"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="2"
        android:background="@color/contact_list">

    </FrameLayout>

    <FrameLayout
        android:id="@+id/frameLayout_contact_details"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="2"
        android:background="@color/contact_details">

    </FrameLayout>
</LinearLayout>